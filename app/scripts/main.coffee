require

  paths:
    "icg": "scripts/vendor/icg"
    "custom": "scripts"

, ['sample'], (sample)->

  # This is where your application logic goes.  To start out we have included sample.coffee as a module....play around with that.
  console.log sample
